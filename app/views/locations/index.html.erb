<h1>Available Locations</h1>

<% locations = @locations.map{|l| [l.name, l.id]} %>

<%= form_tag locations_get_homes_path do %>
  <%= select_tag(:location_id, options_for_select(locations)) %>
  <%= submit_tag("submit") %>
<% end  %>



